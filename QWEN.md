# QuickHack collaboration guide

이 파일은 QuickHack 저장소에서만 적용되는 짧은 작업 계약이다. 일반적인
행동 원칙은 개인 `~/.qwen/QWEN.md`, 제품 의도·설계·구현 절차는
`intent-to-code` Skill과 외부 record store를 기준으로 한다. 같은 내용을 이
파일에 복제하지 않는다.

## 기준과 Skill 선택

- 기능 아이디어, 요구사항, 제품 결정, 영향 분석, 시스템 설계, 구현 계획 또는
  승인된 제품 변경을 다룰 때는 `intent-to-code`를 사용한다.
- 현재 구현 상태는 외부 `NOW/` 기록에서 시작하되, 관련 소스·스키마·테스트와
  실행 결과로 재검증한다. 과거 목표와 설계는 외부 `features/`에서 목표별로
  선택한다.
- `quickhack-sdd`는 migration evidence일 뿐 활성 Skill이나 현재 제품 결정의
  기준이 아니다.
- 다음 구현 목표를 찾을 때 README나 Git 이력에서 목표를 새로 만들지 않는다.
  `long-term-product-roadmap` 기록을 먼저 선택하고 현재 구현만 소스로 재검증한다.
- 현재 동작은 문서의 서술보다 소스, 스키마, 마이그레이션, 테스트와 실행
  결과를 우선해 확인한다. 충돌이 있으면 숨기지 말고 기록한다.
- 저장소 개요는 `README.md`, 개발 흐름은 `CONTRIBUTING.md`에서 필요한
  부분만 읽는다. 두 파일 전체를 매 요청의 상시 컨텍스트로 복제하지 않는다.

## 작업 경계

- 기능 검토·진단 요청은 구현 승인으로 간주하지 않는다.
- 제품 의미가 달라지는 미결정 사항은 사용자의 승인을 받는다. 기술적으로
  되돌릴 수 있는 작은 선택은 저장소 관례를 따라 진행하고 근거를 알린다.
- 관련 없는 기존 변경과 미추적 파일을 보존한다. 임의로 정리, 스테이징,
  커밋, 되돌리기 또는 덮어쓰기를 하지 않는다.
- Linux와 Windows 검증 경계, 외부 운영 환경의 `NOT_APPLICABLE`, SDD의
  승인 상태는 `intent-to-code` 외부 기록의 현재 계약을 그대로 따른다.
- 실물 장비나 실제 외부 계정이 없다는 사실을 실패나 미완료로 부풀리지
  않으며, 실행하지 않은 검증을 `PASS`로 표시하지도 않는다.

## 구현과 검증

- `rg`와 좁은 파일 읽기로 실제 진입점과 호출 경로를 먼저 찾는다.
- 변경은 작은 diff로 나누고 관련 테스트부터 실행한다. 긴 전체 파일 쓰기는
  피하고 기존 구조와 이름을 보존한다.
- `package.json`에서 실제 스크립트를 확인한 뒤 가장 작은 관련 테스트를 먼저
  실행한다. 위험과 범위가 요구할 때만 `npm run typecheck`, 빌드 또는 넓은
  검증으로 확장한다.
- Windows-native 결과는 Windows 증거 없이 검증 완료라고 주장하지 않는다.
  Linux에서 확인한 소스·계약·fixture 결과와 Windows 실행 결과를 구분한다.
- 완료 보고에는 결과, 변경 파일, 실행한 명령과 결과, 생략한 검증의 이유,
  남은 위험과 다음 안전한 행동을 포함한다.

## 협업 태도

- 기본 응답 언어는 한국어로 하고 코드 식별자, 명령과 경로는 원문을 유지한다.
- 막힘이나 실수는 숨기지 않는다. 무엇을 확인했고 어디서 막혔으며 어떤
  결정이나 권한이 필요한지 짧고 구체적으로 보고한다.
- 장시간 작업은 현재 단계와 다음 행동을 간단히 공유한다. 같은 실패를 근거
  없이 반복하지 않는다.
